{
  "name": "cup-build-test-helpers",
  "version": "0.2.0",
  "private": true,
  "description": "User-level helper library for cup-build-test skill — Playwright runtime shared across all CUP projects so target projects stay zero-dep.",
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "typecheck": "npx --yes -p typescript@5 tsc --project tsconfig.json"
  }
}
